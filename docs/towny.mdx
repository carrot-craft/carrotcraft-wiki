---
title: Towny ガイド
description: 町と国家の作り方・管理方法の完全ガイド
icon: castle
---

import { Callout } from 'fumadocs-ui/components/callout';
import { Card, Cards } from 'fumadocs-ui/components/card';
import { Tabs, Tab } from 'fumadocs-ui/components/tabs';
import { Steps, Step } from 'fumadocs-ui/components/steps';
import { Accordions, Accordion } from 'fumadocs-ui/components/accordion';
import { Castle, Users, Coins, MapPin, Crown, Shield, Settings, Flag } from 'lucide-react';

<Callout type="info" title="Towny とは">
土地保護と町づくりのプラグインです。町を作り、住民を招待し、土地を守りながら自分たちの街を発展させましょう。国家を建設して複数の町を束ねることもできます。
</Callout>

<Callout type="warn" title="町には毎日 500 CC の維持費がかかります">
町銀行から自動で引き落とされ、残高不足で支払えない場合は町が削除されます。定期的に `/t deposit` で入金しておきましょう。
</Callout>

## 🎥 動画で見る

更地から町をつくり、仲間を呼び、土地を分けるまでの流れを動画で解説しています。

<div style={{ position: 'relative', paddingBottom: '56.25%', height: 0, overflow: 'hidden', borderRadius: '12px', margin: '1rem 0' }}>
  <iframe
    src="https://www.youtube.com/embed/R0_Nx60_iSc"
    title="CarrotCraft Towny ガイド"
    style={{ position: 'absolute', top: 0, left: 0, width: '100%', height: '100%', border: 0 }}
    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
    allowFullScreen
  />
</div>

## 🏰 町の作り方

<Steps>
<Step>
### 町を作成する

```
/t new <町の名前>
```

- 費用: **4,000 CC**
- 町名は**英数字のみ**使用可能（日本語不可）
- 作成すると、立っている場所がホームブロック（町の中心）になります
</Step>
<Step>
### 町への参加

```
/t join <町名>
```

オープンな町であればコマンドで参加できます。オープンでない町は、町長からの招待が必要です。
</Step>
</Steps>

## 📊 町のレベル

住民数に応じて町のランクが上がり、確保できる土地の上限が増えます。

| 住民数 | 称号 | 町長の肩書き | 最大チャンク |
|--------|------|------------|-------------|
| 1人 | 開拓地 | 開拓者 | 16 |
| 2人 | 集落 | 長 | 32 |
| 6人 | 村 | 村長 | 96 |
| 10人 | 町 | 町長 | 160 |
| 14人 | 大きな町 | 市長 | 224 |
| 20人 | 都市 | 都市長 | 320 |
| 24人 | 大都市 | 首長 | 384 |
| 28人 | メトロポリス | 首領 | 448 |

最大チャンク数は `住民数 × 12` でも計算されます（レベル上限とどちらか大きい方が適用）。

<Callout type="warn">
アウトポスト（飛び地の土地確保）は無効です。町の土地は連続して確保する必要があります。
</Callout>

## 🗺️ 土地の管理

### 土地の確保（Claim）

```
/t claim
```

今いるチャンク（16×16ブロック）を町の土地にします。

- 費用: 1チャンクあたり **500 CC**
- 既存の町の土地に隣接している必要があります
- 他の町から **5チャンク以上** 離れている必要があります
- プレイヤー1人あたり最大 **100プロット** まで所有可能
- `/t claim auto` で移動しながら連続確保も可能

| コマンド | 説明 |
|---------|------|
| `/t claim` | 今いるチャンクを確保 |
| `/t claim auto` | 資金が続く限り自動で確保 |
| `/t claim <半径>` | 自分を中心に指定半径のチャンクをまとめて確保 |
| `/t claim fill` | 町の土地に囲まれた穴を埋める |

### 土地の放棄

| コマンド | 説明 |
|---------|------|
| `/t unclaim` | 今いるチャンクを放棄 |
| `/t unclaim <半径>` | 指定半径をまとめて放棄 |
| `/t unclaim all` | 町の土地をすべて放棄（ホームブロックは残る） |
| `/t cede plot <町名>` | 今いるチャンクを他の町に譲渡 |

### プロットの売買

| コマンド | 説明 |
|---------|------|
| `/plot forsale <価格>` | 土地を売りに出す（`/plot fs` でも可） |
| `/plot forsale circle <半径> <価格>` | 円形の範囲をまとめて売りに出す |
| `/plot forsale rect <半径> <価格>` | 四角形の範囲をまとめて売りに出す |
| `/plot notforsale` | 売却中止（`/plot nfs` でも可） |
| `/plot claim` | 売りに出ている土地を購入 |
| `/plot claim auto` | 近くの売り出し中プロットを自動購入 |
| `/plot unclaim` | 所有しているプロットを返却 |
| `/plot unclaim circle <半径>` | 円形の範囲をまとめて返却 |
| `/plot unclaim rect <半径>` | 四角形の範囲をまとめて返却 |
| `/plot evict` | 町長がプロットの所有者を解除 |
| `/plot evict forsale` | 所有者を解除して即座に売りに出す |

### 町ごと売買する

| コマンド | 説明 |
|---------|------|
| `/t forsale <価格>` | 町ごと売りに出す（町長） |
| `/t notforsale` | 町の売却を中止 |
| `/t buytown <町名>` | 売りに出ている町を購入 |
| `/t merge <町名>` | 指定した町を自分の町に併合する（相手町長の承諾が必要） |

## 👥 住民の管理

| コマンド | 説明 |
|---------|------|
| `/t invite <プレイヤー名>` | 住民を招待 |
| `/t invite sent` | 自分の町が送った招待の一覧 |
| `/t invite removeall` | 送った招待をすべて取り消す |
| `/t add <プレイヤー名>` | 住民を直接追加（相手の承諾が必要） |
| `/t kick <プレイヤー名>` | 住民を追放 |
| `/t leave` | 町から脱退 |
| `/t reslist <町名>` | 住民一覧 |
| `/t online` | オンラインの町メンバー |
| `/t purge <日数>` | 指定日数ログインしていない住民を一括追放 |
| `/t say <メッセージ>` | 町のオンラインメンバー全員にメッセージを送る |

<Callout type="warn" title="42日ログインがないと町から自動で外れます">
  最終ログインから **42日** が経った住民は、町から自動的に退出します。町長が何もしなくても外れるので、長期間サーバーを離れる予定があるときはご注意ください。
</Callout>

### 招待を受けた側の操作

町から招待されたときは、以下のコマンドで応答します。

| コマンド | 説明 |
|---------|------|
| `/invite list` | 自分が受け取っている招待の一覧 |
| `/invite accept <町名>` | 招待を承諾して町に加入 |
| `/invite deny <町名>` | 招待を断る |
| `/invite deny all` | すべての招待を断る |

### 信頼プレイヤー・信頼町

| コマンド | 説明 |
|---------|------|
| `/t trust add <プレイヤー>` | 町全体で信頼するプレイヤーを追加 |
| `/t trust remove <プレイヤー>` | 信頼を解除 |
| `/t trust list` | 信頼プレイヤーの一覧 |
| `/t trusttown add <町名>` | 町ぐるみで信頼する町を追加 |
| `/t trusttown remove <町名>` | 信頼町を解除 |
| `/t trusttown list` | 信頼町の一覧 |

### 牢屋

牢屋プロット（`/plot set jail`）がある町では、住民を投獄できます。

| コマンド | 説明 |
|---------|------|
| `/t jail <プレイヤー>` | 住民を投獄（既定1時間） |
| `/t jail <プレイヤー> <時間>` | 時間を指定して投獄（**最大5時間**） |
| `/t unjail <プレイヤー>` | 釈放する |

<Callout type="info">
保釈金による自主脱出は無効です。釈放は町長・警備ランクの操作か、時間経過を待つ必要があります。
</Callout>

## 👤 自分の設定（/res）

自分自身に関する情報と表示設定です。町長でなくても全員が使えます。

### 情報

| コマンド | 説明 |
|---------|------|
| `/res` | 自分の情報（所属町・所持金・フレンドなど） |
| `/res <プレイヤー名>` | 他のプレイヤーの情報 |
| `/res list` | オンラインの住民一覧 |
| `/res plotlist [ページ]` | 自分が所有しているプロットの一覧 |
| `/res plotlist <名前> [ページ]` | 他プレイヤーのプロット一覧 |
| `/res outlawlist` | 自分がアウトローに指定されている町の一覧 |
| `/res tax` | 自分が払っている税金の内訳 |
| `/res set about <メッセージ>` | 自己紹介文を設定（`clear` で削除） |

### フレンド

| コマンド | 説明 |
|---------|------|
| `/res friend add <プレイヤー>` | フレンド追加（オンラインのみ） |
| `/res friend add+ <プレイヤー>` | オフラインのプレイヤーをフレンド追加 |
| `/res friend remove <プレイヤー>` | フレンド解除 |
| `/res friend list` | フレンド一覧 |
| `/res friend list online` | オンラインのフレンドのみ表示 |
| `/res friend clearlist` | フレンドを全解除 |

### 表示・動作のトグル

<Callout type="info">
土地の境界線表示は、自分の土地と他人の土地を見分けるのにとても便利です。まず `/res toggle constantplotborder` を試してみてください。
</Callout>

| コマンド | 説明 |
|---------|------|
| `/res toggle plotborder` | プロットを跨いだときだけ境界線を表示 |
| `/res toggle constantplotborder` | プロットの境界線を常に表示 |
| `/res toggle townborder` | 町の境界線を常に表示 |
| `/res toggle bordertitles` | 土地の出入りメッセージのON/OFF |
| `/res toggle map` | 移動するたびにマップを自動表示 |
| `/res toggle townclaim` | 移動しながら自動で土地を確保 |
| `/res toggle townunclaim` | 移動しながら自動で土地を放棄 |
| `/res toggle plotgroup` | 確保したプロットを自動でグループに追加 |
| `/res toggle district` | 確保したプロットを自動で地区に追加 |
| `/res toggle bedspawn` | 町スポーンよりベッドを優先する |
| `/res toggle ignoreinvites` | 町への招待を受け取らない |
| `/res toggle ignoreplots` | プロット関連の通知を受け取らない |
| `/res toggle reset` | 上記のトグルをすべて解除 |

### 自分のプロットの設定

自分が所有しているプロット全体に適用される既定の権限です。

| コマンド | 説明 |
|---------|------|
| `/res toggle pvp` | 自分のプロットのPvP |
| `/res toggle fire` | 自分のプロットの延焼 |
| `/res toggle explosion` | 自分のプロットの爆発 |
| `/res toggle mobs` | 自分のプロットのモブスポーン |
| `/res set perm <グループ> <アクション> <on/off>` | 既定の権限を設定 |
| `/res set perm reset` | 所有する全プロットに現在の設定を適用 |

グループは `friend`（フレンド）, `ally`（同盟）, `outsider`（部外者）です。

## 🎖️ 町のランク（役職）

町長は住民にランクを付与できます。各ランクには異なる権限があります。

| ランク名 | 表示 | できること |
|--------|------|----------|
| **vicemayor** | `[副市長]` | 土地確保、招待、設定変更、プロット管理、アウトロー管理 |
| **builder** | `[建築士]` | 町所有の土地での建築、プロット権限変更 |
| **recruiter** | `[広報]` | 住民の招待 |
| **treasurer** | `[会計]` | 税率設定、入出金 |
| **guard** | `[警備]` | 牢屋管理、アウトロー管理 |

ランクの付与:

```
/t rank add <プレイヤー> <ランク名>
/t rank remove <プレイヤー> <ランク名>
```

## ⚙️ 町の設定

### 基本設定

| コマンド | 説明 |
|---------|------|
| `/t set name <名前>` | 町名を変更（**4,000 CC**） |
| `/t set board <メッセージ>` | 町の掲示板メッセージを設定 |
| `/t set spawn` | 町のスポーン地点を現在地に設定 |
| `/t set homeblock` | ホームブロックを現在地に変更 |
| `/t set mapcolor <色名>` | マップ上の町の色を変更 |
| `/t set tag <4文字まで>` | 町の短縮タグを設定（`clear` で削除） |
| `/t set mayor <プレイヤー>` | 町長を他の住民に譲る |
| `/t set title <プレイヤー> <称号>` | 住民の名前の前に称号を付ける |
| `/t set surname <プレイヤー> <称号>` | 住民の名前の後ろに称号を付ける |
| `/t set primaryjail` | 既定の牢屋を設定 |

<Callout type="warn" title="町長の譲渡は取り消せません">
`/t set mayor` を実行すると、その場で町長の権限が相手に移ります。**自分では元に戻せません**。譲る相手をよく確認してから実行してください。
</Callout>

### 税金・価格の設定

| コマンド | 説明 |
|---------|------|
| `/t set taxes <金額>` | 住民への税金を設定 |
| `/t set plottax <金額>` | プロット税を設定 |
| `/t set taxpercentcap <金額>` | 割合課税のときの上限額 |
| `/t set plotprice <金額>` | 町が売るプロットの既定価格 |
| `/t set shopprice <金額>` | ショッププロットの価格 |
| `/t set shoptax <金額>` | ショッププロットの税額 |
| `/t set embassyprice <金額>` | 大使館プロットの価格 |
| `/t set embassytax <金額>` | 大使館プロットの税額 |
| `/t set spawncost <金額>` | 他人が `/t spawn <町名>` で来るときの料金（**上限10 CC**） |

### トグル設定

| コマンド | 説明 |
|---------|------|
| `/t toggle pvp` | PvPのON/OFF |
| `/t toggle fire` | 火の延焼ON/OFF |
| `/t toggle explosion` | 爆発ON/OFF |
| `/t toggle mobs` | モブスポーンON/OFF |
| `/t toggle public` | 町の公開状態（スポーンテレポート許可） |
| `/t toggle open` | 招待なしで参加可能にする |
| `/t toggle taxpercent` | 税金を割合制にする |
| `/t toggle nationzone` | 国家ゾーン（町の周囲の緩衝地帯）のON/OFF |

## 🔐 権限設定

### 町全体の権限

```
/t set perm <グループ> <アクション> <on/off>
```

**グループ:** `resident`（住民）, `nation`（国家メンバー）, `ally`（同盟）, `outsider`（部外者）

**アクション:** `build`（建築）, `destroy`（破壊）, `switch`（コンテナ・スイッチ操作）, `itemuse`（アイテム使用）

例:

```
/t set perm resident build on     ← 住民に建築を許可
/t set perm outsider switch off   ← 部外者のコンテナ操作を禁止
```

### プロット単位の権限

```
/plot set perm <グループ> <アクション> <on/off>
```

### 信頼プレイヤー

```
/plot trust add <プレイヤー>      ← プロットへの信頼追加
/plot trust remove <プレイヤー>
/t trust add <プレイヤー>         ← 町全体への信頼追加
/t trust remove <プレイヤー>
```

## 🏗️ プロットタイプ

プロットには種類を設定でき、それぞれ異なる挙動を持ちます。

```
/plot set <タイプ>
```

| タイプ | 説明 |
|--------|------|
| `default` | 通常のプロット |
| `shop` | ショップ用プロット |
| `arena` | PvPアリーナ（フレンドリーファイア有効） |
| `embassy` | 大使館（町外のプレイヤーが購入可能） |
| `wilds` | 荒野タイプ（特定ブロックのみ採掘可能） |
| `inn` | 宿屋（ベッドスポーン設定可能） |
| `jail` | 牢屋 |
| `farm` | 農場（農作物関連ブロックのみ操作可能） |
| `bank` | 銀行 |

`/plot set reset` で通常のプロットに戻せます。

### プロットのその他の設定

| コマンド | 説明 |
|---------|------|
| `/plot set name <名前>` | プロットに名前を付ける |
| `/plot set minjoindays <日数>` | 町への加入から一定日数が経った住民のみ購入可に（`clear` で解除） |
| `/plot set maxjoindays <日数>` | 加入から一定日数以内の住民のみ購入可に（`clear` で解除） |
| `/plot clear` | プロット内の特定ブロックを一括撤去 |
| `/plot jailcell add` | 牢屋プロットに独房を追加 |
| `/plot jailcell remove` | 独房を削除 |

### プロット単位のトグル

町全体の設定より優先されます。

| コマンド | 説明 |
|---------|------|
| `/plot toggle pvp` | このプロットのPvP |
| `/plot toggle fire` | このプロットの延焼 |
| `/plot toggle explosion` | このプロットの爆発 |
| `/plot toggle mob` | このプロットのモブスポーン |
| `/plot toggle taxed` | このプロットへの課税 |

### 権限の確認と一括設定

| コマンド | 説明 |
|---------|------|
| `/plot perm` | 今いるプロットの権限を表示 |
| `/plot perm gui` | **権限をGUIで設定**（コマンドを覚えなくて済みます） |
| `/plot perm hud` | プロット情報をスコアボードに常時表示 |
| `/plot perm add <プレイヤー>` | 特定プレイヤーだけに権限を与える |
| `/plot perm remove <プレイヤー>` | 個別権限を削除 |
| `/plot set perm reset` | 権限を既定値に戻す |

<Callout type="info" title="GUIが一番早い">
権限設定は `/plot perm gui` を使うのが確実です。クリックだけで建築・破壊・スイッチ・アイテム使用を、住民／同盟／部外者ごとに切り替えられます。
</Callout>

## 📦 プロットグループと地区

複数のプロットをまとめて扱う機能です。**大きな建物や区画をまとめて売買・設定したいとき**に使います。

### プロットグループ（まとめて売買・設定）

<Steps>
<Step>
### グループを作る

```
/plot group new <グループ名>
```

今いるプロットが新しいグループの最初のメンバーになります。
</Step>
<Step>
### プロットを追加する

`/res toggle plotgroup` をONにしてから土地を確保すると、自動でグループに追加されます。手動なら各プロットで `/plot group add <グループ名>` を実行します。
</Step>
<Step>
### まとめて操作する

```
/plot group forsale <価格>
/plot group set <タイプ>
```

グループ全体が1つの単位として売買・設定されます。
</Step>
</Steps>

| コマンド | 説明 |
|---------|------|
| `/plot group new <名前>` | グループを作成 |
| `/plot group add <名前>` | 今いるプロットをグループに追加 |
| `/plot group remove` | 今いるプロットをグループから外す |
| `/plot group rename <新しい名前>` | グループ名を変更 |
| `/plot group delete` | グループを削除 |
| `/plot group set <タイプ>` | グループ全体のプロットタイプを設定 |
| `/plot group set perm ...` | グループ全体の権限を設定 |
| `/plot group toggle ...` | グループ全体のトグル設定 |
| `/plot group forsale <価格>` | グループごと売りに出す |
| `/plot group notforsale` | グループの売却を中止 |
| `/plot group trust add <プレイヤー>` | グループ全体に信頼プレイヤーを追加 |
| `/plot group trust remove <プレイヤー>` | 信頼を解除 |
| `/plot group perm gui` | グループの権限をGUIで設定 |

### 地区（District）

町の中を「商業区」「住宅区」のように区分けして管理するための機能です。売買はできず、**整理・表示のための区分**です。

| コマンド | 説明 |
|---------|------|
| `/plot district new <名前>` | 地区を作成 |
| `/plot district add <名前>` | 今いるプロットを地区に追加 |
| `/plot district rename <新しい名前>` | 地区名を変更 |
| `/plot district remove` | 今いるプロットを地区から外す |
| `/plot district delete` | 地区を削除 |

## 🏦 町の銀行

| コマンド | 説明 |
|---------|------|
| `/t deposit <金額>` | 町銀行に入金 |
| `/t deposit all` | 所持金を全額入金 |
| `/t deposit <金額> <町名>` | 他の町の銀行に入金する（支援・寄付） |
| `/t withdraw <金額>` | 町銀行から出金（町長・会計のみ） |
| `/t withdraw all` | 残高を全額出金 |
| `/t bankhistory <件数>` | 入出金の履歴を確認 |
| `/t baltop` | 町の中でお金を持っている住民のランキング |
| `/t buy bonus <数>` | ボーナスチャンクを購入 |

### 維持費

<Callout type="warn">
町は毎日 **500 CC** の維持費がかかります。町銀行から自動で引き落とされます。維持費を払えないと町が削除されるので注意してください。
</Callout>

## 🚀 テレポート

| コマンド | 説明 | 費用 |
|---------|------|------|
| `/t spawn` | 自分の町にテレポート | 無料 |
| `/t spawn <町名>` | 他の町にテレポート（公開町のみ） | 最大10 CC |
| `/n spawn` | 国家の首都にテレポート | 無料 |

国家メンバーの町へのテレポート: **5 CC**、同盟国の町: **10 CC**

<Callout type="info">
テレポートにはクールダウン **30秒** があります。
</Callout>

## 🚫 アウトロー

町から追放されたプレイヤーを「アウトロー」に指定できます。

```
/t outlaw add <プレイヤー>
/t outlaw remove <プレイヤー>
```

## 👑 国家（Nation）

### 国家の建設

```
/n new <国家名>
```

- 費用: **150,000 CC**
- 町長のみ実行可能
- 国家名は**英数字のみ**

### 国家のレベル

| 総住民数 | 称号 | 王の肩書き | ボーナスチャンク |
|----------|------|----------|---------------|
| 0人 | 国 | 指導者 | +10 |
| 10人 | 連邦 | 伯爵 | +20 |
| 20人 | 領 | 公爵 | +40 |
| 30人 | 王国 | 王 | +60 |
| 40人 | 帝国 | 皇帝 | +100 |
| 60人 | 大帝国 | 大皇帝 | +140 |

ボーナスチャンクは、国家に属する町が追加で確保できるチャンク数です。

### 国家コマンド

| コマンド | 説明 |
|---------|------|
| `/n` | 自分の国家の情報 |
| `/n <国家名>` | 指定した国家の情報 |
| `/n list` | 国家一覧 |
| `/n online` | オンラインの国家メンバー |
| `/n add <町名>` | 町を国家に招待 |
| `/n kick <町名>` | 町を国家から追放 |
| `/n join <国家名>` | 公開されている国家に加入（町長） |
| `/n leave` | 国家から脱退（町長） |
| `/n say <メッセージ>` | 国家メンバー全員にメッセージを送る |
| `/n merge <国家名>` | 国家の合併を申請 |
| `/n spawn` | 国家の首都にテレポート |

### 国家への招待・加入

| コマンド | 説明 |
|---------|------|
| `/n invite sent` | 自国が送った招待の一覧 |
| `/n invite received` | 自国が受け取っている招待の一覧 |
| `/t invite received` | （町側）国家からの招待を確認 |
| `/t invite accept <国家名>` | 国家の招待を承諾する |
| `/t invite deny <国家名>` | 国家の招待を断る |

### 外交

| コマンド | 説明 |
|---------|------|
| `/n ally add <国家名>` | 同盟を申請 |
| `/n ally remove <国家名>` | 同盟を解消 |
| `/n ally accept <国家名>` | 同盟の申請を承諾 |
| `/n ally deny <国家名>` | 同盟の申請を断る |
| `/n ally sent` | 自国が送った同盟申請 |
| `/n ally received` | 自国が受け取った同盟申請 |
| `/n enemy add <国家名>` | 敵国として宣言 |
| `/n enemy remove <国家名>` | 敵国の指定を解除 |

<Callout type="info">
同盟は**相手の承諾が必要**です。`/n ally add` を実行しただけでは成立せず、相手国が `/n ally accept` するまで保留になります。敵国指定は一方的に実行できます。
</Callout>

### 国家の設定

| コマンド | 説明 |
|---------|------|
| `/n set king <プレイヤー>` | 王を変更 |
| `/n set capital <町名>` | 首都を変更 |
| `/n set name <名前>` | 国家名を変更 |
| `/n set spawn` | 国家スポーン地点を設定 |
| `/n set spawncost <金額>` | 他国民が来るときの料金 |
| `/n set board <メッセージ>` | ログイン時に表示されるメッセージ |
| `/n set tag <4文字まで>` | 国家の短縮タグ（`clear` で削除） |
| `/n set mapcolor <色名>` | マップ上の国家の色 |
| `/n set taxes <金額>` | 加盟町から徴収する国税 |
| `/n set taxpercentcap <金額>` | 割合課税のときの上限額 |
| `/n set title <プレイヤー> <称号>` | メンバーに称号を付ける |
| `/n set surname <プレイヤー> <称号>` | メンバーの名前の後ろに称号を付ける |
| `/n toggle open` | 招待なしで加入可能にする |
| `/n toggle neutral` | 中立国にする（戦争に巻き込まれない・**有料**） |
| `/n toggle taxpercent` | 国税を割合制にする |

### 国庫

| コマンド | 説明 |
|---------|------|
| `/n deposit <金額>` | 国庫に入金 |
| `/n deposit all` | 所持金を全額入金 |
| `/n withdraw <金額>` | 国庫から出金 |
| `/n withdraw all` | 残高を全額出金 |
| `/n bankhistory <件数>` | 入出金の履歴 |
| `/n baltop` | 国家メンバーの所持金ランキング |

### 国家ランクの操作

```
/n rank add <プレイヤー> <ランク名>
/n rank remove <プレイヤー> <ランク名>
/n ranklist
```

### 国家ランク

| ランク名 | 表示 | できること |
|--------|------|----------|
| **viceroy** | `[総督]` | 招待、同盟、敵国設定、設定変更、ランク管理 |
| **diplomat** | `[外交官]` | 同盟・敵国の管理 |
| **recruiter** | `[国家広報]` | 町の招待 |
| **treasurer** | `[国家会計]` | 入出金、税率設定 |
| **helper** | — | 町の追加 |

### 国家の維持費

<Callout type="warn">
国家は毎日 **1,000 CC** の維持費がかかります。払えないと国家が解散します。
</Callout>

## 📋 情報確認コマンド

| コマンド | 説明 |
|---------|------|
| `/t` | 自分の町の情報 |
| `/t <町名>` | 指定した町の情報 |
| `/t list` | 町の一覧 |
| `/t online` | 町のオンラインメンバー |
| `/t here` | 今いる場所の町情報 |
| `/t ranklist` | 町のランク一覧 |
| `/n` | 自分の国家の情報 |
| `/n list` | 国家の一覧 |
| `/n townlist <国家名>` | 国家の町一覧 |
| `/n allylist <国家名>` | 同盟国一覧 |
| `/n enemylist <国家名>` | 敵国一覧 |
| `/res <プレイヤー名>` | 住民の情報 |
| `/t nearby` | 近くにある町を最大10件表示 |
| `/t plots` | 町のプロット一覧（種類・収益つき） |
| `/t plotgrouplist <町名>` | 町のプロットグループ一覧 |
| `/t outlawlist <町名>` | 町のアウトロー一覧 |
| `/t allylist <町名>` | 町の同盟一覧 |
| `/t enemylist <町名>` | 町の敵対一覧 |
| `/n ranklist` | 国家メンバーのランク一覧 |

### 一覧の並べ替え

`/t list` と `/n list` は並べ替えができます。**移住先や同盟先を探すときに便利です。**

| コマンド | 説明 |
|---------|------|
| `/t list by resident` | 住民が多い順に町を表示 |
| `/t list by online` | オンライン人数が多い順 |
| `/t list by open` | 招待なしで入れる町を優先表示 |
| `/t list by public` | 公開されている（テレポート可能な）町を優先表示 |
| `/t list by balance` | 町の資産が多い順 |
| `/t list by townblocks` | 土地が広い順 |
| `/t list by forsale` | 売りに出ている町 |
| `/t list by founded` | 設立が古い順 |
| `/t list by name` | 名前順 |
| `/n list by resident` | 総人口が多い順に国家を表示 |
| `/n list by towns` | 加盟町が多い順 |
| `/n list by online` | オンライン人数が多い順 |
| `/n list by open` | 加入自由な国家を優先表示 |

<Callout type="info" title="町を探しているなら">
まだどこにも所属していない人は `/t list by open` で、招待なしで入れる町を探せます。そのあと `/t join <町名>` で加入できます。
</Callout>

## 🗺️ マップと全般コマンド（/towny）

```
/towny map
```

チャットにASCIIマップが表示されます。自分の周囲の土地の所有状況を確認できます。

| コマンド | 説明 |
|---------|------|
| `/towny map` | 周囲の土地の所有状況をチャットに表示 |
| `/towny map hud` | マップをスコアボードに常時表示する |
| `/towny prices` | **町の作成・土地確保・維持費などの料金一覧** |
| `/towny time` | 次の税金・維持費の徴収までの残り時間 |
| `/towny top residents` | 住民数の多い町・国家のランキング |
| `/towny top land` | 土地の広い住民・町のランキング |
| `/towny allowedblocks` | 建築・破壊が許可されているブロック一覧 |
| `/towny switches` | スイッチ扱いになるブロック一覧（`switch` 権限の対象） |
| `/towny itemuse` | アイテム使用扱いになるアイテム一覧（`itemuse` 権限の対象） |
| `/towny wildsblocks` | `wilds` プロットで採掘できるブロック一覧 |
| `/towny plotclearblocks` | `/plot clear` で撤去されるブロック一覧 |
| `/towny universe` | サーバー全体の統計（住民・町・国家・土地の総数） |

<Callout type="info" title="料金がわからなくなったら">
`/towny prices` を実行すれば、その時点で有効な料金がすべて表示されます。このページの金額表記と食い違う場合は、**サーバー内の `/towny prices` が正しい値**です。
</Callout>

## 💬 チャットチャンネル

町や国家のメンバーだけに聞こえるチャンネルで会話できます。

| コマンド | 説明 |
|---------|------|
| `/tc <メッセージ>` | 町チャット（町のメンバーのみ） |
| `/nc <メッセージ>` | 国家チャット（国家のメンバーのみ） |
| `/ac <メッセージ>` | 同盟チャット（同盟国のメンバー） |
| `/g <メッセージ>` | 全体チャット |
| `/tc` | メッセージなしで実行すると、そのチャンネルに固定される |

<Callout type="info">
`/tc` のようにメッセージを付けずに実行すると、以降の発言がすべてそのチャンネルに送られます。全体チャットに戻すには `/g` を実行してください。
</Callout>

## ❓ よくある質問

<Accordions>
<Accordion title="町が削除されるのはどんな時？">
町銀行の残高が不足して維持費（500 CC/日）を払えない場合に削除されます。定期的に `/t deposit` で入金しておきましょう。
</Accordion>
<Accordion title="他の人が自分の町の土地を壊せる？">
権限設定（`/t set perm`）で制御できます。デフォルトでは住民のみが建築・破壊可能で、部外者は操作できません。
</Accordion>
<Accordion title="町の最大チャンク数を増やすには？">
住民を増やすか、`/t buy bonus <数>` でボーナスチャンクを購入（1チャンク25 CC）してください。国家に参加するとボーナスチャンクも得られます。
</Accordion>
<Accordion title="MajinCraftアイテムとTowny権限">
MajinCraftのカスタムアイテム（ヴェインピッケル、ティンバーアックス、ブラストアロー等）は**Towny権限に従います**。建築権限がない場所ではブロックの破壊・爆発が発動しません。モブ操作系のアイテム（ポーション、レインボーシアー等）もモブ操作権限に準拠します。自分の町の土地か荒野でのみ使用できます。
</Accordion>
</Accordions>
